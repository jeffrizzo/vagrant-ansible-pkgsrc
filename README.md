This is intended to create a setup where testing pkgsrc changes is
straightforward.

Tested using vagrant under MacOS, using a simple
`vagrant up`
at the commandline.  Assumes you have Ansible installed already.
